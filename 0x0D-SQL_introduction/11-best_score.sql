-- Lists all rows of the tbl second_table
-- From the data hbtn_0c_0
-- Display score and name
-- Ordered by score
-- With score >= 10
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
